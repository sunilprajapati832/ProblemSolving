
```python
#Libraries
import  requests // for get response method , BeautifulSoup
import pandas
import numpy
import matplotlib // .pyplot
import seaborn 
import scipy // .stats
import plotly //.express
import joblib  // to load saved model pipelines
import datetime // from datetime 
import logging // # Setup logging    logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(message)s')

## Machine Learnings & Advanced Libraries
import RandomForestRegressor // from sklearn.ensemble 
import Pipeline // from sklearn.pipeline 
import StandardScaler // from sklearn.preprocessing
import train_test_split // from sklearn.model_selection 
import sklearn
import sqrt // from math 
import os // # Create output directory for charts        // os.makedirs("outputs/eda_charts", exist_ok=True)
import train_test_split // from sklearn.model_selection 
import LinearRegression // from sklearn.linear_model 
import mean_squared_error, mean_absolute_error, r2_score // from sklearn.metrics 
import OneHotEncoder // from sklearn.preprocessing 
import ColumnTransformer // from sklearn.compose 
import xgboost
import folium
import json
import geopandas //  # Step 2: Load the NYC Taxi Zone GeoJSON file   geojson_path = "sample_taxi_zones.geojson"   zones_gdf = gpd.read_file(geojson_path)
import warnings // warnings.filterwarnings("ignore")
import StandardScaler // from sklearn.preprocessing 
import ( accuracy_score, roc_auc_score, confusion_matrix, roc_curve ) // from sklearn.metrics 
import LogisticRegression // from sklearn.linear_model 
import DecisionTreeClassifier // from sklearn.tree 
import RandomForestClassifier // from sklearn.ensemble 
import create_engine // from sqlalchemy     engine = create_engine(f"mysql+pymysql://{username}:{password}@{host}/{database}")
import squarify // # Plot treemap      squarify.plot(sizes=department_salary, label=department_salary.index, alpha=0.7, color=['#ff9999', '#66b3ff', '#99ff99', '#ffcc99', '#c2c2f0'])
import mysql.connector // # Database connection    conn = mysql.connector.connect( host="localhost", user="root", password="12345", database="employee_data" )
```


import pandas as pd
import numpy as np 

data = {"Name" : ["Sunil", "Sushil", "Swami"],
"Age": [30, 40, 50],
"Department": ["HR","IT", "Finance"]}




